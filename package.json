{
  "scripts": {
    "build": "NODE_ENV=production webpack && libexec/fix-non-ascii-contentscript && cd dist && zip -FS -r ../translate_onhover ./* -x test.html test.js",
    "build_dist": "NODE_ENV=production webpack",
    "dev": "webpack --watch",
    "test": "npx http-server dist & npx wait-on http-get://localhost:8080 && npx cypress run --spec 'cypress/integration/*'",
    "lint": "eslint --ignore-pattern dist --ignore-pattern cypress ."
  },
  "devDependencies": {
    "@babel/core": "^7.4.4",
    "@babel/plugin-transform-classes": "^7.4.4",
    "babel-loader": "^8.0.5",
    "clean-webpack-plugin": "^1.0.0",
    "copy-webpack-plugin": "^4.6.0",
    "cypress": "^3.3.2",
    "eslint": "^5.11.0",
    "eslint-plugin-cypress": "^2.2.1",
    "http-server": "^0.11.1",
    "npx": "^10.2.0",
    "wait-on": "^3.2.0",
    "webpack": "^4.28.2",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "jquery": "^3.5.0",
    "xregexp": "^4.2.0"
  }
}
